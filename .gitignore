# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules

# Build artifacts
dist
dist-ssr
*.local
*.pack
*.tsbuildinfo

# Next.js
.next/
out/

# Payload
dev.db
dev.db-journal
payload-types.ts
# importMap é artefato gerado por `payload generate:importmap` (roda no vercel-build.sh).
# Versioná-lo causava bug: cópia stale com handler de storage errado (Vercel Blob vs S3)
# sombreava a versão correta gerada no build. NÃO versionar.
src/app/(payload)/admin/importMap.js

# Environment variables
.env
.env.local
.env.*.local
.env.development.local
.env.test.local
.env.production.local

# Vercel
.vercel

# AIOX duplicata e backups locais
meu-projeto/
*-backup/
*-backup.git/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Uploads de mídia do Payload CMS (não versionar arquivos de upload)
public/media/*
!public/media/.gitkeep

# Material de origem do esboço Lovable do redesign do guia (já portado para src/app/guia-eleicoes-2026)
campaign-bloom-studio-main/

# Config local do Claude Code (arquivo vivo, contem permissoes/segredos locais) - nunca versionar
.claude/settings.local.json
