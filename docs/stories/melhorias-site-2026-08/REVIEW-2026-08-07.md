# Revisão de Qualidade — 2026-08-07

Revisão dupla (PO consistência + Arquiteto viabilidade), cruzada com o código real. Consolidado dos achados e o que foi ajustado nas stories.

## Críticos (ajustados)

| # | Achado | Ajuste aplicado |
|---|--------|-----------------|
| C1 | Redirect de slug (S02/AC11): `middleware.ts` não consulta `redirects`; matcher não cobre `/blog/[slug]`; create exige `super-admin` mas usuários são `editor` | S02: redirect vira escopo obrigatório — wiring no middleware + catch-all OU `redirect()` na page; criação via **server action com Local API privilegiada** (`overrideAccess`), não pela permissão do editor |
| C2 | Alt obrigatório retroativo (S01) cria dependência circular com S07 e trava posts legados | S01: `Media.alt` required **só no create**; barramento real da imagem no editor é S03; removida a falsa dependência de "alt legado antes" |
| C3 | S01 (varredura de links) e S05 (normalização de heading) escrevem o mesmo `conteudo_html` | S01 roda a varredura **primeiro**; S05 assume esse estado; **snapshot único** compartilhado; dry-run em ambos |

## Importantes (ajustados)

| # | Achado | Ajuste |
|---|--------|--------|
| I1 | `article-toc.ts` (`addHeadingIds`) só trata H2/H3 (regex `h[23]`, `TocItem.level: 2\|3`) | S01/T5 amplia para `h[2-6]` + tipo, junto do fix da âncora |
| I2 | Âncora corta no `.slice(0,60)` (id **e** link do TOC) | S01/T5: cortar na última fronteira de hífen antes de 60 |
| I3 | `Media.alt` required no Payload não barra `<img>` inserida no editor | Expectativa corrigida: barramento efetivo em S03/T2 (modal de imagem) |
| I4 | Vídeo mobile: `display:none` ainda baixa o `<source>` | S06/AC2.1c: render-gating por breakpoint (não montar o `<video>`), não CSS |
| I5 | Cache headers podem colidir com `vercel.json` (histórico de quebra) | S06/AC2.3a: headers **no `next.config.ts`**, não tocar `vercel.json` |
| I6 | 2.2b/2.2d (Clarity, GA4/Ads dup) sem DoD verificável; dependem de acesso GTM + decisão funil | S06: movidos para fora do DoD de código; bloqueados por pendência externa rastreada |
| I7 | Campos novos exigem migration (Postgres `push:false`, `payload.config.ts`) | Nota global: S02 (`resumo_busca`), S04 (`faq`) geram migration antes do deploy |

## Menores (registrados, não bloqueiam)

- Métricas objetivas p/ "sem regressão de travamento" (S03) e TBT alvo (S06). 
- Breakpoint do vídeo (`<768px` = só poster).
- Consolidar contagem de redação (42 títulos busca vs 14 H1 vs 2 H2) numa tabela p/ Ferraz (S07).
- `/contato` (S07/AC10): definir que o texto entra em bloco acima do form, sem quebrar a conversão.

## Confirmações da revisão (o diagnóstico do INDEX é fiel ao repo)

- Duplicação da marca: `layout.tsx:40` template + `blog/[slug]/page.tsx:66` string → marca 2× + "Blog". Fix `| Blog` válido.
- `html-sanitize.ts:43` `external = /^https?:\/\//` — correção mesmo-domínio viável ali.
- `ArticleSchema` existe (`SchemaOrg.tsx:40`) e NÃO plugado; Organization/WebSite já injetados (`layout.tsx:93-94`); Breadcrumb não existe.
- TipTap `variant:'full'` já traz Table/Figure/YoutubeEmbed — sem dependência npm nova p/ editor.
- Clarity e FB Pixel ausentes de `src/` — confirmam que são tags no GTM.

---
*— Revisão consolidada por Orion (aios-master) · 2026-08-07*
